mutation updatePayoutForReservation($payoutId: Int!, $reservationId: Int!) {
    updatePayoutForReservation(
        payoutId: $payoutId
        reservationId: $reservationId
    ) {
        status
        errorMessage
    }
}