query validatePromoCode($code: String!) {
    validatePromoCode(code: $code) {
        status
        errorMessage
        result {
            id
            code
            promoValue
            deductionType
        }
    }
}